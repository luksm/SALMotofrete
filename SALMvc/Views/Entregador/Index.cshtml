@model IEnumerable<SALClassLib.Masterdata.Model.Entregador>

@{
    ViewBag.Title = "Listagem de Entregadores";
    Layout = "~/Views/Shared/_Utopia.cshtml";
    Html.RenderPartial("~/Views/Shared/_FlashPartial.cshtml");
}

<div class="row" style="border-bottom: 1px solid black;">
    <h2>
        Entregadores
        <a href="@Url.Action("Create", "Entregador")" class="btn btn-primary pull-right" type="button"><i class="icon-user icon-white"> </i>  Novo Entregador</a>
    </h2>
</div>

<div class="row">
    <table class="table table-hover table-striped">
        <thead>
            <tr>
                <th>@Html.DisplayNameFor(model => model.Nome)</th>
                <th>@Html.DisplayNameFor(model => model.Sobrenome)</th>
                <th>@Html.DisplayNameFor(model => model.Cpf)</th>
                <th>@Html.DisplayNameFor(model => model.Rg)</th>
                <th>@Html.DisplayNameFor(model => model.Cnh)</th>
                <th class="actions">Ações</th>
            </tr>
        </thead>
        <tbody>
@foreach (var item in Model) {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.Nome)</td>
                <td>@Html.DisplayFor(modelItem => item.Sobrenome)</td>
                <td>@Html.DisplayFor(modelItem => item.Cpf)</td>
                <td>@Html.DisplayFor(modelItem => item.Rg)</td>
                <td>@Html.DisplayFor(modelItem => item.Cnh)</td>
                <td class="actions">
                    <a href="@Url.Action("Edit", "Entregador", new { id=item.Id })" class="btn btn-small btn-info"><i class="icon-pencil icon-white"> </i>  Editar</a>
                    <a href="@Url.Action("Details", "Entregador", new { id=item.Id })" class="btn btn-small btn-info"><i class="icon-info-sign icon-white"> </i>  Detalhes</a>
                    <a href="@Url.Action("Delete", "Entregador", new { id=item.Id })" class="btn btn-small btn-info"><i class="icon-remove-circle icon-white"> </i>  Excluir</a>
                </td>
            </tr>
}
        </tbody>
    </table>
</div>

